#include <stdio.h>
int main()
{
    int i, j,k, rows = 5;
    for (int i = 0; i <= rows - 1; i++){
        for (int j = 0; j <= i; j++){
            printf(" ");
        }

        for (int k = 0; k <= rows - 1 - i; k++) { 
            printf("*"  " ");
        }
    printf("\n");
    }

}
